# AutoGen multi-agent framework
pyautogen>=0.2,<0.3

# Config
python-dotenv>=1.0.0

# Log Retrieval
requests

# MCP server + Slack webhook (Python 3.9 compatible, no fastmcp needed)
flask

# MCP client (requires Python 3.10+)
mcp
